apply plugin: 'groovy'

repositories {
    mavenCentral()
}

dependencies {
    compile fileTree(dir: 'idea-libs', include: '*.jar')

    testCompile('org.spockframework:spock-core:0.7-groovy-2.0') {
        exclude module: 'groovy-all'  // comes from IntelliJ SDK
    }
}